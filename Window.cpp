#include <windows.h>
/*
#include "Window.h"

Window::SetOnTop()
{
	EnumWindows(EnumWindowsProc, );
}

BOOL CALLBACK EnumWindowsProc(HWND hwnd, LPARAM lParam)
{

	char className[80];
	char title[80];
	
	GetClassName(hwnd, className, sizeof(className));
	GetWindowText(hwnd, title, sizeof(title));

	if (std::string::npos != ((string) className).find("GomPlayer1.x")) {
		SetWindowOnTop(hwnd);
		CloseCurrentWindow(hwnd);
		
		cout << "Window title: " << title << endl;
		cout << "Class name: " << className << endl << endl;
	}

	return TRUE;
}
*/
